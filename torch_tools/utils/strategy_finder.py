# from torch_tools.utils import TensorboardLogs
#
#
# def find_best_model(tensorboard_logs_path: str, tag: str, best_fn: min):
#     logs = TensorboardLogs(tensorboard_logs_path)
#
#     for metric in logs.scalars(tag):
#         if
#
